git init

git add <filename>

git commit -m "your message"

git status

git log

git restore <filename>

git rm --cached <filename>

git remote -v

git add origin <git hub repo link>

git remote set-url https://<git PAT >@github.com/<repo link>

git push 

this is some new changes

blah blah blah!
